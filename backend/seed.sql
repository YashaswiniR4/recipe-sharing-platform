-- Seed template for backend. Note: seeding auth.users must be done via Supabase Auth or API.

-- Sample recipes (replace USER_UUID with a real user id from auth.users)
-- insert into recipes (title, description, ingredients, category, image_url, user_id)
-- values ('Sample Pancakes','Easy pancakes','flour,egg,milk','Breakfast','https://via.placeholder.com/400', 'USER_UUID');

-- Sample ratings (replace USER_UUID and RECIPE_UUID)
-- insert into ratings (user_id, recipe_id, rating) values ('USER_UUID','RECIPE_UUID',5);

-- Sample favorites
-- insert into favorites (user_id, recipe_id) values ('USER_UUID','RECIPE_UUID');

-- Use these as templates after creating users via Supabase Auth console or API.
